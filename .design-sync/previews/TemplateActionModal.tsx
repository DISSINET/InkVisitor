import React from "react";
import { TemplateActionModal } from "dissinet.ddb.client";

export const Default = () => (
  <TemplateActionModal
    onClose={() => {}}
    onUse={() => {}}
    onInstantiate={() => {}}
  />
);
