{
  "name": "dissinet.ddb.import",
  "version": "1.0.0",
  "description": "dissinet.cz The first part of the DDB infrastructure that parser spreadsheet tables to the DDB ontology.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "googleapis": "^52.1.0"
  }
}
